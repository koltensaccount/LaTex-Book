% Page geometry tuned for wide inner margin and fixed left margin notes
\usepackage[
  paperwidth=8.5in,
  paperheight=11in,
  twoside=false,
  left=2.5in,
  right=0.75in,
  top=0.25in,
  bottom=1in,
  headheight=26pt,
  includeheadfoot,
  marginparwidth=1.6in,
  marginparsep=15pt
]{geometry}

\makeatletter
\@mparswitchfalse
\makeatother

% Font configuration for body, headings, and captions
\usepackage{fontspec}
\newfontfamily\mainfont{JetBrains Mono}
\newfontfamily\sansfont{Helvetica}
\newfontfamily\seriffont{Times New Roman}

\newcommand{\fontbody}{\seriffont}
\newcommand{\fontheader}{\sansfont\bfseries}
\newcommand{\fonttitle}{\sansfont\bfseries\LARGE}
\newcommand{\fontmono}{\mainfont}
\newcommand{\fontcaption}{\seriffont\small}
\newcommand{\fontemph}{\seriffont\itshape}

\AtBeginDocument{%
  \fontbody
  \reversemarginpar
}

% Color palette shared across styled elements
\usepackage[dvipsnames]{xcolor}
\definecolor{Primary}{rgb}{0.18,0.24,0.50}
\definecolor{Accent}{rgb}{0.96,0.97,0.99}
\definecolor{RuleGray}{gray}{0.80}
\definecolor{Highlight}{rgb}{0.80,0.40,0.20}
\hypersetup{
  colorlinks=true,
  linkcolor=Primary,
  urlcolor=Primary,
  citecolor=Primary
}

% Header and footer styling
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead{}%
\fancyhead[L]{%
  \ifodd\value{page}
    \fontheader\MakeUppercase{\leftmark}%
  \else
    \thepage
  \fi
}
\fancyhead[R]{%
  \ifodd\value{page}
    \thepage
  \else
    \fontheader\MakeUppercase{\leftmark}%
  \fi
}
\renewcommand{\headrulewidth}{0.4pt}
\setlength{\headheight}{26pt}
\fancypagestyle{plain}{\pagestyle{fancy}}
